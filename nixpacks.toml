# ===========================================
# Nixpacks Build Configuration
# ===========================================
# Builds Python backend + Node.js webapp + PWA

[phases.setup]
nixPkgs = ["python311", "gcc", "libffi", "openssl", "nodejs_20"]

[phases.install]
cmds = ["bash build.sh"]

[start]
cmd = "python run_all.py"
